@model MyCollection.Web.Models.PropertyManagerViewModel
@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Property</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddProperty">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ManagerId" />

            <partial name="_Property" />

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-primary" />
                <a asp-action="Details" asp-route-id="@Model.ManagerId" class="btn btn-outline-dark">Back to Collector</a>
            </div>
        </form>
    </div>
</div>